- checkpoint-integrity-check: verify ledger/doc projection consistency and emit report
- checkpoint-reconcile: rewrite docs checkpoint projection from checkpoint-ledger current
- planning-context-probe: read-only gateway before planning-with-files or planning-file init/update; returns ABSENT|ALIGNED|CONFLICT and one nextCommand; CONFLICT recovery is admission-reconcile -Task "<task>"
- suggestion-triple-sync: same-round receipt writer; when -SuggestionId/-ProgressId already exists, it upserts matching ledger/progress rows instead of appending duplicate ids.
- admission-reconcile|single-writer-reconcile: converge runtime-task-state + checkpoint-ledger + session-fence + runtime/doc/closeout lanes with one unified admission report (explicit `-Task` required)
- session-fence blocker rule: only unexpired session-fence views can block single-writer admission; expired fence files must be reported as `sessionFenceExpiredCount`, and self-held active fences are evidence, not foreign ownership.
- same-task stale writer recovery: `admission-reconcile` / `single-writer-reconcile` may clear foreign owner/session-fence residue only when every active view points to the requested task and no material ownership claim exists; multi-task or live-claim evidence remains fail-closed.
- admission-probe-normalize|admission-probe-rewrite: normalize historical admission probes to the current trace/planning schema
- runtime-writer-trace-report -At <time>: evidence-only/read-only trace for ownerSession/action/txId around a target timestamp from admission probes + operation ledger; when writer-lane contention is observed, emit blocker evidence and continue without writer-lane acquire
- governance-control-plane-v2 -Task "<task>" [-Phase ASK_ONLY|INTAKE|EXECUTE|VERIFYING|CLOSEOUT|RECOVERY_ONLY] [-FastAction <action>]: root-cause blocker card for fail-closed consistency. It snapshots runtime/checkpoint/lane/fence/staged/receipt state and classifies `CONSISTENT|RECOVERY_ALLOWED|DEFERRED_RECEIPT|HARD_STOP|UNSAFE_CONFLICT` with one canonical recovery command.
- governance-control-plane-v2 command template: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action governance-control-plane-v2 -Task "<task>" -Phase RECOVERY_ONLY -FastAction <action>`.
- ops-route-plan -Task "<task>" [-TargetPaths <paths>] [-Mode Staged] [-GateProfile Fast] [-SuggestionId SUG-YYYYMMDD-NNN] [-PersistLatestReports]: classify current scope as `READONLY_PLAN|SINGLE_WRITER_FAST|MULTI_WRITER_SHARDED|HEAVY_GOVERNANCE`, list required gates and skipped heavy work, and emit acceleration evidence.
- ops-route-plan DB/schema rule: SQL folders, schema/migration names, mapper/entity/domain-sensitive paths, and config or build files escalate to `HEAVY_GOVERNANCE` and require `schema-contract-check`; non-sensitive staged scope must emit skip telemetry instead of running the heavy check.
- governance-token-report -Task "<task>" [-LookbackDays 7] [-Json]: emit Token-Minimal Compact Evidence Card and write full evidence to `docs/治理/最新态/governance-token-report-latest.json`.
- governance-token-report command template: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action governance-token-report -Task "<task>" [-LookbackDays 7] [-Json]`.
- command-template-lint -CommandText "<command>": execution-before-template lint for shell commands; PowerShell Maven property tokens must be quoted, e.g. `mvn test "-DfailIfNoTests=false"`; `Get-ChildItem -Filter` accepts one string only, so multiple patterns must use `-Include`/`Where-Object` instead of `-Filter 'a','b'`.
- closeout residual evidence actions: closeout-frozen-snapshot-check freezes target/evidence/staged fingerprint/SUG-B mapping; sidecar-outbox-check proves doc-mutating sidecar work is deferred; lease-heartbeat-check proves the closeout lease TTL floor; sidecar-encoding-check proves UTF-8 sidecar logs; all-debt-ledger-check records Mode=All historical debt outside ordinary Staged/Fast closure; release-gate-debt-check records release-gate latest mismatch/BLOCKED as debt without refreshing release-gate on ordinary Staged/Fast closeout; workspace-debt-ledger-check buckets dirty/staged workspace debt without staging/deleting/reverting; workspace-maintenance-cleanup-check emits maintenance cleanup evidence (Plan by default, Strict applies).
- auto-maintenance leaf actions: task-start calls progress-doc-auto-compact and governance-doc-auto-compact through RCCP leaf scripts; task-end force-runs both as closeout-gate hard checks before task-close, then runs auto-commit-govern as best-effort tail maintenance. Progress/governance compaction conflict checks are scoped to each managed document plus its archive, while auto-commit-govern reports STALE_TARGET_SCOPE when `.claude/target-files.txt` no longer intersects current non-noise changes.
- rccp-declare/rccp-checkpoint/rccp-closeout-snapshot/rccp-closeout-fast/rccp-closeout-sidecar: ops facade actions that forward RCCP task events, checkpoints, closeout snapshots, snapshot-only fast closeout, and async sidecar evidence to `scripts/rccp/rccp.ps1` while preserving the unified command entry.
- rccp facade command templates: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action rccp-declare -Task "<task>" -RiskClass NORMAL -TargetPaths <path1>,<path2> [-Json]`; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action rccp-checkpoint -Task "<task>" -CurrentState "<state>" -NextStep "<next>" -EvidencePaths <path1>,<path2> [-Json]`; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action rccp-closeout-snapshot -Task "<task>" -SuggestionId SUG-YYYYMMDD-NNN -TargetPaths <path1>,<path2> -EvidencePaths <path1>,<path2> [-Json]`; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action rccp-closeout-fast -Task "<task>" -TargetPaths <path1>,<path2> -EvidencePaths <path1>,<path2> [-Json]`; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action rccp-closeout-sidecar -Task "<task>" -SuggestionId SUG-YYYYMMDD-NNN -ProgressId B.NNN -TargetPaths <path1>,<path2> -EvidencePaths <path1>,<path2> [-Json]`.
- thin-entry check: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action thin-entry-check -Task "<task>" [-Strict] [-Json]`; validates wrapper-only entry, externalized dispatch, policy bundle, evidence schema, task graph schema, and retired tombstones.
- rccp fast closeout rule: `rccp-closeout-fast` validates an immutable RCCP snapshot only; it must not refresh latest evidence, compact context, or run suggestion receipt audit on the main chain. `rccp-closeout-sidecar` may refresh slow evidence asynchronously after the snapshot path has passed.
- multisession invisible parallel rule: business files may run in parallel only after file-level ownership is disjoint; shared control-plane state (`runtime-task-state`, checkpoint-ledger, session-fence, writer lanes, latest evidence, planning projections, and closeout snapshot) must flow through session-local event -> single reducer projection -> evidence freeze -> closeout arbitration queue.
- multisession recovery order: when a new task is blocked by ACTIVE_TASK_DRIFT or SINGLE_WRITER_EVIDENCE_CONFLICT, first run `runtime-writer-trace-report -At <time>` for attribution, then `admission-reconcile -Task "<task>"`; retry `task-bootstrap` only after runtime state is IDLE or aligned. Do not manually clear runtime files.
- closeout arbitration rule: parallel sessions submit closeout requests; only Main/reducer may run `rccp-closeout-snapshot`, `rccp-closeout-fast`, `closeout-atomic`, or `task-end`. Slow/latest evidence refresh belongs to sidecar after the frozen snapshot passes.
- sidecar outbox rule: async closeout sidecars must not rewrite `docs/治理/建议池.md` or `docs/开发执行与优化进度.md`; they record deferred doc-mutating intents for the main writer lane.
- Runtime Control Plane V2 rule: fail-closed remains mandatory for risk actions, but recovery/read-only diagnostics use controlled exception lanes. `DEFERRED_RECEIPT` never means closed; it means accepted pending evidence only.
- suggestion backlog rule: ordinary non-Issue `Mode=Staged + GateProfile=Fast` closeout runs backlog aging/report only and must not require global pending=0; Issue/Release/Deep/All/Strict lanes may keep backlog-zero fail-closed behavior.
- task-reentry-probe -Task "<task>": read-only two-phase probe (`ACTIVE_TASK_EXECUTABILITY -> TARGET_TASK_REENTRY`) for lease/admission blockers before `task-bootstrap`; no runtime-writer lane acquire
- existing-capability-probe -Task "<task>" -Why "<request text>": read-only evidence probe for ECDAP routing before answering 方案/优化/评估 requests about existing capabilities; verdicts are `DELTA_ANSWER_REQUIRED|EXISTING_CAPABILITY_CONFIRMED|GREENFIELD_ALLOWED|BLOCKED_EVIDENCE`
- existing-capability-answer-shape-check -Why "<request text>" -AnswerText "<answer>" [-AnswerPath <path>] [-Strict]: read-only ECDAP answer-shape lint; when probe verdict is `DELTA_ANSWER_REQUIRED`, missing delta fields are reported as `GREENFIELD_ANSWER_REGRESSION`
- project-onboard -Task "<task>" [-ProjectConfigPath rccp.project.json] [-ContractPath docs/治理/策略/rccp-project-contract.json] [-Strict]: validates the project thin config, local runtime/evidence roots, required actions, and emits an explicit next command with `-Task`.
- project-governance-check -Task "<task>" [-ProjectConfigPath rccp.project.json] [-ContractPath docs/治理/策略/rccp-project-contract.json] [-Strict]: verifies project action dispatch, leaf contracts, local evidence isolation, and Governance Kit route readiness.
- rccp-kit-compat-check -Task "<task>" [-ProjectConfigPath rccp.project.json] [-ContractPath docs/治理/策略/rccp-project-contract.json] [-Strict]: validates project contract version compatibility, canonical entry readiness, and rollback-profile compatibility.
- control-plane-ux-root-cause-buckets -Task "<task>" -Why "<request text>": read-only evidence probe that compresses many control-plane UX painpoints into 8 fixed root-cause buckets, each with one primary solution, one probe command, one regression evidence path, and one acceptance gate
- pre-dispatch hard gate: `task-start` / `task-bootstrap` / `task-switch` / `fast-exec` / `closeout-commit` / `closeout-fastlane` / `painpoint-p0-fastlane` / `drift-auto-heal` / `opensec-phaseb-fastlane` / `pre-commit-smoke*` auto-run `task-reentry-probe`; only phase2 PASS can continue.
- pre-dispatch probe cache: short TTL memo is keyed by `targetTask + runtime/ledger/lane/staged fingerprint` to skip high-frequency duplicate probe runs safely.
- runtime-writer-fairness-regression: dedicated same-session renewal-storm regression for forced-yield + cooldown assertions (`forced-yield -> cooldown block -> cooldown recovery`)
- checkpoint-chaos-test: run drift-injection and auto-repair regression test
- runtime-admission-journal-check: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action runtime-admission-journal-check [-Strict]`; verifies partitioned runtime admission journal trace completeness, orphan PREPARE status, and current runtime/checkpoint drift
- runtime-admission-journal-replay: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action runtime-admission-journal-replay [-Strict]`; replays partitioned runtime admission journal into read-only per-partition projection evidence without mutating runtime/checkpoint state
Hard rule: direct recovery/runtime query entrypoints require explicit `-Task`; inferring `resume-check`, `ownership-check`, `closeout-*`, or project-kit validation targets from runtime/checkpoint state is blocked.
Hard rule: a new admission/runtime direct entrypoint is complete only when action entry hard gate, shared help wording, and blocker recovery command stay aligned in the same round.
Hard rule: `resume-check` / `closeout-check` / `task-end` share one front-loaded hard-gate triad: `env-guard` readiness + `activeTask` drift + staged-scope drift; none of the three may be deferred to final closeout only.
Hard rule: any command-template change must update both `scripts/README.md` and this file (`scripts/help/admission-runtime-actions.txt`) in the same round.
Hard rule: staged `doc-check` enforces `DOC_TEMPLATE_DUAL_SYNC_GATE` + `DOC_HELP_VERSION_STAMP_GATE`; template drift or missing help source-version-stamp entrypoint is blocked before commit.
Hard rule: manual keep script list in `scripts/README.md` must stay synchronized with script-hygiene policy when a new manual utility script is retained instead of deleted.
Hard rule: `gate` phase-2 summary must expose template homology verdict as `模板同源性=PASS/FAIL`, sourced from doc-check snapshot.
- template-change required flow: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action doc-check -Mode Staged` -> `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action gate-debt -Mode Staged -GateProfile Fast`
- external reporting single-source: only use `gate` phase-2 summary line `模板同源性=PASS/FAIL`.
- governance-stale-candidates-check (bucketed default): `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action governance-stale-candidates-check -Task "<task>" -SuggestionId SUG-YYYYMMDD-NNN`
- governance-stale-candidates-check (weekly full coverage): `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/rccp/rccp.ps1 -Action governance-stale-candidates-check -Task "<task>" -SuggestionId SUG-YYYYMMDD-NNN -FullScan -Strict`
- stale-candidates arg template: `[-OutDir docs/治理/最新态] [-GovernanceStaleMinInactiveDays 90] [-ColdBucketCount 6] [-HotBatchSize 100] [-ColdBatchSize 120] [-BatchPauseMs 25] [-BucketPauseMs 120] [-FullScan] [-Strict]`
- rule: default remains `hot full + cold rotate` (no `-FullScan`); weekly governance window should use `-FullScan -Strict`.
- rccp-root-cause-bucket-regression-check strict rule: `runnerStrictOk` must be derived from `root-cause-probe-runner-latest.json` with `strict=true` and `pass=true`; `runnerStrictOk=false` blocks `-Strict` V3-B confirmation.
