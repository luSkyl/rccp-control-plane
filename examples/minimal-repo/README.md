# Minimal RCCP Adopter

This example represents a small repository that only wants RCCP admission,
ownership, and closeout evidence.

Install RCCP into the repository root:

```powershell
pwsh -NoProfile -File ..\..\install.ps1 -TargetRoot .
pwsh -NoProfile -File .\rccp.ps1 -Action help
```
